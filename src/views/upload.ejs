<!DOCTYPE html>
<html>
<head>
    <title>Upload | AI Interview Simulator</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Cdefs%3E%3ClinearGradient id='g' x1='0' x2='1' y1='0' y2='1'%3E%3Cstop stop-color='%233a7bfd'/%3E%3Cstop offset='1' stop-color='%236b5bff'/%3E%3C/linearGradient%3E%3C/defs%3E%3Crect width='64' height='64' rx='14' fill='%230f0f10'/%3E%3Cpath d='M18 42l10-20 8 12 10-8' stroke='url(%23g)' stroke-width='5' fill='none' stroke-linecap='round' stroke-linejoin='round'/%3E%3Ccircle cx='48' cy='18' r='4' fill='%236b5bff'/%3E%3C/svg%3E">
    <link rel="stylesheet" href="/styles/app.css">
</head>
<body>
<%- include('partials/navbar'); %>

<div class="pg-hero">
    <div class="pg-orb pg-orb--blue"></div>
    <div class="pg-orb pg-orb--purple"></div>
    <div class="section-shell pg-hero__inner">
        <div>
            <p class="eyebrow">Resume scoring lab</p>
            <h1 class="pg-hero__title">Pressure-test the resume<br>before the interview starts.</h1>
            <p class="pg-hero__sub">Upload your PDF or DOCX, paste a real job description, and get a maximum-strictness score with optional company research layered in.</p>
        </div>
    </div>
</div>

<section class="pg-body">
    <div class="section-shell upload-shell">

        <aside class="upload-intro">
            <div class="upload-feature-list">
                <div class="upload-feature glass">
                    <strong>Role-fit breakdown</strong>
                    <span>Score based on evidence, impact, completeness, and project depth.</span>
                </div>
                <div class="upload-feature glass">
                    <strong>Maximum strictness</strong>
                    <span>Every score uses the harshest evidence-based hiring bar by default.</span>
                </div>
                <div class="upload-feature glass">
                    <strong>Optional web context</strong>
                    <span>Add company and role context for a more situational evaluation.</span>
                </div>
            </div>
        </aside>

        <div class="upload-form-wrap">
            <div class="glass upload-form-card">
                <div style="margin-bottom: 8px;">
                    <p class="eyebrow">Resume input</p>
                    <h2 style="margin: 4px 0 6px; font-size: 1.5rem; font-weight: 800; letter-spacing: -0.02em;">Upload and score</h2>
                    <p style="color: var(--text-subtle); font-size: 0.92rem; margin: 0;">Keep the job description specific — the tighter the target, the more useful the score.</p>
                </div>

                <form action="/upload" method="POST" enctype="multipart/form-data">
                    <label>Resume (PDF or DOCX)</label>
                    <input type="file" name="resume" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" required>

                    <label>Company Name</label>
                    <input type="text" name="company" placeholder="e.g., Google, Deloitte..." required>

                    <label>Job Description</label>
                    <textarea name="jobDescription" rows="8" maxlength="4000" placeholder="Paste job description here (up to 4000 characters)..." required></textarea>

                    <div class="setup-card upload-controls-card">
                        <div class="setup-card__head">
                            <div>
                                <p class="eyebrow">Scoring controls</p>
                                <h4>Strict scoring defaults</h4>
                            </div>
                            <p class="muted">Resume scoring runs at the most skeptical setting by default.</p>
                        </div>
                        <div class="setup-switches">
                            <label class="switch-row">
                                <span>
                                    <strong>Web research</strong>
                                    <small class="muted">Pull external company and role context before scoring.</small>
                                </span>
                                <span class="switch">
                                    <input type="checkbox" id="web-search-enabled" name="webSearchEnabled" checked>
                                    <span class="switch-ui"></span>
                                </span>
                            </label>
                        </div>
                    </div>

                    <button id="upload-submit" type="submit" class="btn-primary full-width lp-btn-glow" style="margin-top: 8px;">
                        Score Resume
                    </button>
                </form>
            </div>
        </div>
    </div>
</section>

<div id="loading-overlay" class="loading-overlay hidden">
    <div class="loader"></div>
    <p>Scoring your resume...</p>
</div>

<script>
    const uploadForm = document.querySelector('form');
    const overlay = document.getElementById('loading-overlay');
    const submitBtn = document.getElementById('upload-submit');
    uploadForm.addEventListener('submit', () => {
        overlay.classList.remove('hidden');
        submitBtn.disabled = true;
        submitBtn.textContent = 'Uploading...';
    });
</script>

<%- include('partials/footer'); %>
</body>
</html>
